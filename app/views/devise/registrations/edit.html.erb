<!DOCTYPE html>
<html>
<head>
  <title>Edit Profile</title>
    <style type="text/css">
       .form-group.last { margin-bottom:0px; }
    </style>
</head>
<body>

<!-- Rendering Menu -->
  <%= render :partial => 'layouts/menu' %>

<!-- <h2>Edit <%= resource_name.to_s.humanize %></h2> -->
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>
  <div class="container"> 

    <div class="row-fluid">

      <div class="panel panel-info ">

        <div class="panel-heading">Edit Your Profile</div>

        <div class="panel-body">

          <form class="form-inline" role="form">

            <div class="form-group">
               <small><label class="col-sm-3 control-label">Email</label></small>
                <div>
                  <%= f.email_field :email, :autofocus => true , label: false %>
                </div>
            </div>

            <div class="form-group">
              <div>
                <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                <div >
                  Currently waiting confirmation for: <%= resource.unconfirmed_email %>
                </div>
                <% end %>
              </div>
            </div>

            <div class="form-group">
                <small><label class="col-sm-3 control-label">Password</label></small>
                <div >
                  <%= f.password_field :password, :autocomplete => "off"  %>
                </div>  
            </div>

             <div class="form-group">
                <small><label class="col-sm-3 control-label">Confirm Password</label></small>
                <div>
                   <%= f.password_field :password_confirmation, :autocomplete => "off"  %>
                </div>  
            </div>

            <div class="form-group">
                <small><label class="col-sm-3 control-label">New Password</label></small>
                <div >
                 <%= f.password_field :current_password, :autocomplete => "off" %>
              </div>  
            </div>

            <div class="form-group last">
              <div class="col-sm-offset-3 col-sm-9">
                <%= f.submit "Update" ,  class: "btn btn-default pull-left"%>
              </div>
            </div>
          </form>
        </div>

        <div class="panel-footer"> 
              Cancel your account 
              <%= button_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete %>
              <%= link_to "Back", :back%> 
        </div>

      </div>
    </div>
  </div>

<% end %>
</body>
</html>














           

           
       


